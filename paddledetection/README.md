# The official paddledetection repository is used for training & inference. See: https://paddlepaddle.github.io/PaddleOCR/latest/en/ppstructure/model_train/train_layout.html

# The config.yaml file will be placed inside `configs/picodet/legacy_model/application/layout_analysis` directory. And the train.sh script will be run for training.

# Dataset format is PublayNet format. Make sure the directories match inside the config.yaml file.